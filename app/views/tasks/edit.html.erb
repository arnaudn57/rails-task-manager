<div class="container">
<div class="check-square">
<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label :completed %>
 <%= f.check_box :completed %>
  <div class="btn btn-primary"> <%= f.submit %>  </div>
<% end %>

<%= link_to "Retour vers la liste", tasks_path%>

</div>

</div>
